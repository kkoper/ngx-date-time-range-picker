language: node_js
node_js:
  - "8"
sudo: true
dist: trusty

branches:
  only:
  - master

script:
  - npm run test
  - ng build --prod

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  local_dir: dist
  on:
    branch: master